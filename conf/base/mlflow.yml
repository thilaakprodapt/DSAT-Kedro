# MLFlow configuration for DSAT
# Configuration for kedro-mlflow integration

# MLFlow tracking settings
# MLFlow tracking settings
server:
  mlflow_tracking_uri: mlruns

tracking:
  experiment:
    name: DSAT_Experiments

ui:
  port: "5100"
  host: 0.0.0.0

